🔁 Updated Development Prompt
Project: Humphrey Visual Field 24-2 Full Threshold Simulation (VR-Compatible)

🔧 Bug Fixes
Fix visual test issues:

The fullscreen option should remain active throughout the test.

Ensure compatibility with mobile/VR (e.g., Google Cardboard).

🧠 Visual Field Test Features
Implement the 24-2 Grid (54 test points):

Use standard ±24° layout.

54 points excluding the blind spot.

Always display a central fixation cross.

Stimulus Presentation:

Goldmann Size III (0.43°) stimulus.

White stimulus on a gray background.

Show for 500 ms.

Accept any screen tap within 50–1500 ms after stimulus appears — no need to click on the stimulus itself.

Hide the stimulus after 500 ms.

4–2 Staircase Logic:

Start at 24 dB.

If seen, decrease by 4 dB.

If missed, increase by 4 dB.

Once bracketed, switch to ±2 dB steps.

Continue until each point has a final threshold value.

Test Persistence:

Test should resume from the last point if paused or reloaded.

🧑‍⚕️ Doctor-Side Live Monitoring
Use Flask-SocketIO/WebSockets:

Show in real-time:

Which point is being tested

Current decibel value

Whether the response was ✅ seen or ❌ missed

Live heatmap/grid view of progress.

📊 Reliability Indices (Calculated in Backend):
Calculate:

False Positives (FP): patient taps when no stimulus shown

False Negatives (FN): patient fails to tap for bright stimulus at a previously seen point

Fixation Loss (FL): patient taps for blind spot test

Show percentages and raise alerts if:

FP > 15%

FN > 25%

FL > 20%

📁 Data Storage
Before test:

Collect Name, Age, Patient ID

After test:

Save data to .xlsx using pandas + openpyxl

For each point:

x, y position

dB level

Response (seen/missed)

Reaction time

Point type: Regular / FP / FN / FL

Results Summary:

Grayscale map of sensitivity

FP, FN, FL %

Threshold Deviation (TD), Pattern Deviation (PD)

Mean Deviation (MD), Pattern Standard Deviation (PSD)

GHT result (upper vs lower zones)

📱 Mobile & VR Fixes
Fullscreen on Mobile:

Use meta viewport:

html
Copy
Edit
<meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
Trigger requestFullscreen() on test start.

Maintain fullscreen throughout.

VR Layout:

Center fixation and stimuli for stereoscopic/VR layout.

Compatible with Google Cardboard (2D fallback supported).

Touch anywhere = response.